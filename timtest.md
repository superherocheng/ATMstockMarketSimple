请你帮我在导航栏新增一个模块，名为可视化分析，模块设计参照以下标准，请你思考并让我选择每一个关键步骤应该用什么方向去执行。

```markdown
# 行业ETF四象限因子构建与IC分析指导

## 1. 因子生成逻辑

### 原始指标
- **份额趋势 (Flow)**：刻画资金流入流出强度
  - 计算方式：`份额变化率 = (当日份额 / N日前份额 - 1)`，N取10或20
  - 或采用线性回归斜率：`Flow = (最近N日份额序列的OLS斜率) / 过去N日平均份额`（标准化趋势）
- **价格动量 (Mom)**：刻画价格趋势
  - 计算方式：`Mom = 当日收盘价 / M日前收盘价 - 1`，M取20（或60）
  - 推荐波动率调整：`Mom_adj = Mom / 过去60日收益率标准差`

### 因子转换
在每期截面上，对Flow和Mom分别做标准化处理（Z-score或排序百分位），得到 `Z_Flow` 和 `Z_Mom`。

### 最终因子值 (Factor)
连续交互因子（保留更多信息）**
```
Factor = Z_Flow * Z_Mom   （值越大，越偏向强势买入/弱势卖出）
```
亦可同时保留Z_Flow和Z_Mom作为独立因子，进行多元回归检验。

---

## 2. IC分析框架

### 未来收益率定义
- `Forward_Ret_H = 未来第H日收盘价 / 当日收盘价 - 1`，H可取1、5、10、20
- （也可用对数收益率）

### 每期IC计算
在每个交易日t：
1. 获取全部行业ETF的当日因子值 `Factor_i,t`
2. 获取各ETF未来H日收益率 `Ret_i,t+H`
3. 计算横截面 **Spearman Rank IC**：
   - `IC_t = spearmanr(Factor_t, Forward_Ret_H_t)`
4. 生成IC时间序列：`[IC_1, IC_2, ..., IC_T]`

### 评价指标
- **IC Mean**：`mean(IC_序列)`  → 因子方向与预测能力的平均强度
- **ICIR (Information Coefficient Information Ratio)**：`IC_Mean / std(IC_序列)`  → 因子稳定性
- **IC胜率**：`IC序列中大于0的比例`
- **因子衰减 (IC Decay)**：对不同H（1,5,10,20,40,60日）分别计算IC Mean，绘制IC均值随持有期变化的曲线。

### 分组回测（辅助验证）
- 每期按因子值将ETF分为5组或4组（对应四象限），计算每组等权未来收益，验证单调性及象限2、3的反转特性。

---

## 3. 可视化图表清单

以下图表适合用于网站展示，直观体现因子表现和投资指导。

1. **因子分布直方图**：展示某一时点截面因子值的分布（若为分类因子可用柱状图）。
2. **IC序列曲线**：横轴时间，纵轴IC值，添加均值线和±2标准差区间。
3. **IC衰减图**：横轴为预测期H（天），纵轴为对应IC均值，观察因子有效性衰减速度。
4. **四象限收益热力图**：以Z_Flow为Y轴、Z_Mom为X轴，颜色表示该象限下一期平均收益，直观验证理论。
5. **分组累计收益曲线**：各象限（或因子分位组）的净值走势对比，突出象限1与象限3的差异。
6. **因子滚动ICIR**：滚动窗口（如60日）计算ICIR，观察因子稳定性变化。
7. **行业配置权重建议图**（进阶）：基于当前因子值映射到各ETF的建议权重（如仅做多因子值最高的前N个行业）。

---

## 4. 产出总结与投资指导

根据上述分析，系统应输出一份文字总结，包含：
- 因子有效性结论：IC均值、ICIR是否显著，衰减期多久。
- 四象限验证：象限1是否确实正收益，象限4负收益，象限2、3是否存在反转效应。
- 当前市场状态下的配置建议：基于最新因子值，列出处于“强势买入”和“逆势抄底”象限的行业ETF，供投资者参考。
- 风险提示：若市场整体动量或资金流趋同，因子区分度下降，建议加配宽基对冲或降低仓位。

---

## 5. 给vibe coding的提示

- 请用Python实现，pandas计算指标，scipy计算spearmanr，echarts生成交互图表。
- 输出以上图表的JSON数据，供前端渲染。
- IC计算务必使用**未来信息对齐**，防止使用未来数据。
```